name: worker

on:
  push:

jobs:
  logic:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        adventure: ["loyalty", "glorious", "explore", "duty", "saddle"]
        claw: ["dull", "worst", "female", "vacuum"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar hK
